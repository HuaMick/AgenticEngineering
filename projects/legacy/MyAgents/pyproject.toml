[project]
name = "myagents-workspace"
version = "0.1.0"
description = "Workspace root for MyAgents and Agent-GCPtoolkit"
requires-python = ">=3.11"

[tool.uv.workspace]
members = ["MyAgents", "Agent-GCPtoolkit"]
