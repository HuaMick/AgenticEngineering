plan_name: "Execute_Plan.yml Purification - Extract Assets and Refine Process"
worktree: "/home/code/myagents/MyAgentsGuidance-guidance-refinement-v1"
branch: "guidance-refinement-v1"
created: "2025-12-25"
completed: "2025-12-25"
objective: "Fully eliminate Execute_Plan.yml by integrating its remaining logic into specialized orchestrators (orchestration-build, orchestration-guidance) and shared guidelines. Establish orchestration-build/process.mmd as the single source of truth for build workflows."

# FRICTION ANALYSIS - ALL RESOLVED
# =================================
# 1. Knowledge Encapsulation Gap: RESOLVED - Validation logic extracted to validation-strategy.yml
# 2. Missing Guideline: RESOLVED - validation-strategy.yml created with canonical format
# 3. Purpose Ambiguity: RESOLVED - Context qualifiers added where needed
# 4. Verbosity: RESOLVED - Execute_Plan.yml reduced by 31% (111 → 77 lines)
# 5. Orchestration Confusion: RESOLVED - Kept Execute_Plan.yml as generic executor (no routing signpost)

phases:
  - name: "phase-1-extract-assets"
    description: "Extract implementation details from Execute_Plan.yml into reusable assets and verify existing definitions"
    status: "completed"
    tasks:
      - id: "1.1"
        description: "Create assets/guidelines/validation-strategy.yml following canonical guideline structure"
        status: "completed"
        acceptance_criteria:
          - "assets/guidelines/validation-strategy.yml exists with canonical structure (guideline.name, description, inputs, rules, examples, related_guidelines)"
          - "Includes validation_types section distinguishing mechanical (exit codes, file existence, syntax) vs semantic (implementation correctness, domain logic)"
          - "Includes decision_criteria section on when to use bash steps vs spawn an agent"
          - "Includes agent_delegation section covering patterns from Execute_Plan.yml"
          - "Guideline is concise and reusable"

      - id: "1.2"
        description: "Verify assets/definitions/plans.yml explicitly covers TWO-LEVEL COMPLETION and COPY-NOT-MOVE patterns with status values"
        status: "completed"
        acceptance_criteria:
          - "plans.yml explicitly documents these status values: 'planning', 'active', 'partially_completed', 'fully_completed'"
          - "plans.yml distinguishes 'item completion' (requires MOVE to completed/) from 'folder completion' (uses COPY to preserve completed/ as archive)"
          - "plans.yml updated if any gaps found relative to Execute_Plan.yml implementation"

      - id: "1.3"
        description: "Register validation-strategy.yml in assets/guidelines/manifest.yml"
        status: "completed"
        acceptance_criteria:
          - "manifest.yml contains new entry for validation-strategy.yml"
          - "Entry includes: file, name, purpose, key_concepts, definitions, related fields"
          - "Entry follows manifest structure pattern from existing guidelines"

  - name: "phase-2-purify-process"
    description: "Refactor Execute_Plan.yml to be concise and reference extracted assets"
    status: "completed"
    tasks:
      - id: "2.1"
        description: "Refactor Execute_Plan.yml steps to reference extracted assets and plans.yml"
        status: "completed"
        acceptance_criteria:
          - "Redundant lifecycle and validation logic removed from steps"
          - "Steps reference assets/definitions/plans.yml for completion patterns"
          - "Steps reference assets/guidelines/validation-strategy.yml for validation approach"

      - id: "2.2"
        description: "Add context qualifiers to Execute_Plan.yml where terminology is ambiguous"
        status: "completed"
        acceptance_criteria:
          - "Context qualifiers added only where terms like 'build' or 'test' could be ambiguous between guidance and code contexts"
          - "References to build/test agents remain intact"
          - "Process serves BOTH guidance AND code execution contexts effectively"

  - name: "phase-3-audit-and-verify"
    description: "Validate the refactored process"
    status: "completed"
    tasks:
      - id: "3.1"
        description: "Run test-final-output audit on the refactored Execute_Plan.yml"
        status: "completed"
        agent_type: "test-final-output"
        audit_result: "PASS - All 5 tasks verified, 31% size reduction, no content loss"

  - name: "phase-4-eliminate-generic-executor"
    description: "Fully eliminate Execute_Plan.yml by extracting remaining logic to shared guidelines, integrating into specialized orchestrators, and deleting the transitional artifact."
    status: "completed"
    planning_iterations: 7
    planning_approved: "2025-12-27"
    execution_started: "2025-12-27"
    execution_completed: "2025-12-27"
    tasks:
      - id: "4.1"
        description: "Extract Agent-Task Alignment and Escalation Policy to assets/guidelines/orchestration-policy.yml"
        status: "completed"
        acceptance_criteria:
          - "File created at assets/guidelines/orchestration-policy.yml"
          - "Contains rule: Agent-Task Alignment Verification with matrix check pattern"
          - "References docs/agent-role-scope-matrix.md as the source of agent scope definitions"
          - "Contains rule: Escalation Policy with 'offload to agents' principle"
          - "Contains rule: Plan Status Management (update status, completion patterns)"
          - "Contains rule: Success Criteria Communication (bullet points, success/failure reporting)"
          - "Follows canonical guideline structure (name, description, inputs, rules, examples, related_guidelines)"
          - "References assets/definitions/plans.yml for completion patterns"

      - id: "4.2"
        description: "Extract User Simulator Testing Pattern to assets/guidelines/user-story-testing.yml"
        status: "completed"
        acceptance_criteria:
          - "File created at assets/guidelines/user-story-testing.yml"
          - "Contains rule: User Simulator Pre-requisites (Docker image build)"
          - "Contains rule: User Story Extraction (from docs/services/userstories/)"
          - "Contains rule: Blind Testing Pattern (pass story definition, not file paths)"
          - "Contains rule: Dual Environment Testing (local + Docker)"
          - "Follows canonical guideline structure"
          - "Includes user story structure: persona, starting_state, journey, success_criteria"

      - id: "4.3"
        description: "Register new guidelines in assets/guidelines/manifest.yml"
        status: "completed"
        acceptance_criteria:
          - "orchestration-policy.yml entry exists in manifest"
          - "user-story-testing.yml entry exists in manifest"
          - "Both entries follow manifest structure (file, name, purpose, key_concepts, related)"

      - id: "4.4"
        description: "Update orchestration-build/process.mmd to reference new orchestration-policy.yml"
        status: "completed"
        acceptance_criteria:
          - "Comment added referencing orchestration-policy.yml for agent spawning rules"
          - "File remains valid mermaid flowchart syntax"
          - "All existing nodes remain unchanged (git diff shows only additions)"

      - id: "4.5"
        description: "Update orchestration-build/inputs.yml to include new guidelines"
        status: "completed"
        acceptance_criteria:
          - "orchestration-policy.yml added to inputs list"
          - "user-story-testing.yml added to inputs list"
          - "validation-strategy.yml already present (from Phase 1)"
          - "Each entry has location and description fields"

      - id: "4.6"
        description: "Update orchestration-guidance/process.mmd to reference orchestration-policy.yml"
        status: "completed"
        acceptance_criteria:
          - "Comment added referencing orchestration-policy.yml for agent spawning"
          - "File remains valid mermaid flowchart syntax"

      - id: "4.7"
        description: "Update orchestration-guidance/inputs.yml to include new guidelines"
        status: "completed"
        acceptance_criteria:
          - "orchestration-policy.yml added to inputs list"
          - "user-story-testing.yml added to inputs list"
          - "Each entry has location and description fields"

      - id: "4.8"
        description: "Verify complete knowledge migration - all Execute_Plan.yml content exists elsewhere"
        status: "completed"
        acceptance_criteria:
          - "Agent-Task Alignment logic migrated to orchestration-policy.yml"
          - "User Simulator pattern migrated to user-story-testing.yml"
          - "Escalation policy migrated to orchestration-policy.yml"
          - "Plan status management migrated to orchestration-policy.yml"
          - "Success criteria communication migrated to orchestration-policy.yml"
          - "ALL 11 Execute_Plan.yml inputs migrated to orchestration inputs"

      - id: "4.9"
        description: "Remove Execute_Plan.yml from agents/orchestration/manifest.yml"
        status: "completed"
        acceptance_criteria:
          - "orchestration-execute entry removed from sub_agents list"
          - "Execute_Plan.yml path removed from manifest"
          - "orchestration-build and orchestration-guidance entries remain intact"

      - id: "4.10"
        description: "Update entrypoints/README.md to remove stale Execute_Plan.yml documentation references"
        status: "completed"
        acceptance_criteria:
          - "Execute_Plan.yml reference removed from README (no _execute.yml entrypoint exists)"
          - "Documentation reflects orchestration-build as the implementation orchestrator"
          - "_teach.yml and _plan.yml references remain intact"

      - id: "4.11"
        description: "Delete processes/Execute_Plan.yml file"
        status: "completed"
        dependencies: ["4.9"]
        acceptance_criteria:
          - "Execute_Plan.yml file deleted"
          - "processes/ directory still exists"
          - "Manifest no longer references the deleted file (prerequisite)"

      - id: "4.12"
        description: "Update all remaining Execute_Plan.yml references in plan documentation"
        status: "completed"
        acceptance_criteria:
          - "No active references to Execute_Plan.yml exist outside archived plans"
          - "Live plan READMEs reference orchestration-build or orchestration-guidance"

      - id: "4.13"
        description: "Final verification - no broken references and orchestrators are complete"
        status: "completed"
        agent_type: "test-final-output"
        audit_result: "PASS - All 6 criteria verified. Orchestrators complete, guidelines well-formed, knowledge migrated."
        acceptance_criteria:
          - "orchestration-build/process.mmd contains complete build workflow"
          - "orchestration-guidance/process.mmd contains complete guidance workflow"
          - "New guidelines are well-formed and registered"
          - "Execute_Plan.yml is deleted and not referenced in active code/assets"
          - "Orchestration manifest reflects new architecture"

