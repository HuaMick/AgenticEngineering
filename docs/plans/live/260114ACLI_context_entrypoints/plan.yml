entrypoint:
  name: "CLI Context Compilation"
  objective: "Implement JIT compilation for agent inputs using Claude Code Hooks."
  status: "backlog"

phases:
  - name: "Phase 1: Compiler Logic"
    description: "Implement `agentic inputs compile` and `agentic agents compile`."
    steps:
      - "Create `cmd_compile` in `inputs.py` to flatten `inputs.yml` to Markdown."
      - "Create `cmd_compile_all` to batch process `manifest.yml`. "
      - "Implement checksum/timestamp checks for `lazy` mode."
    status: "deferred"

  - name: "Phase 2: Hook Integration"
    description: "Configure Claude Code hooks for JIT execution."
    steps:
      - "Define `UserPromptSubmit` hook in settings."
      - "Hook command: `agentic agents compile --lazy`."
      - "Verify zero-latency impact on user session."
    status: "deferred"

  - name: "Phase 3: Validation"
    description: "Verify that compiled agents work in Claude Code."
    steps:
      - "Run compiler."
      - "Inspect generated MD files."
      - "Run a test session."
    status: "pending"
