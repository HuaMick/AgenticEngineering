# Test Plan: redesign_question_notification_tmux_tui
# Plan ID: 260221TU
# Created: 2026-02-21

name: "redesign_question_notification_tmux_tui - test"
worktree_path: "/home/code/AgenticEngineering"
branch: "main"
status: "pending"
priority: "high"

objective: |
  Validate the tmux TUI redesign with unit tests for new components and regression
  tests for the unchanged question CLI commands. Ensure no ntfy imports remain
  anywhere in the question system after cleanup.

phases:

  - id: "T01"
    name: "Unit tests for QuestionTUI"
    status: "pending"
    tasks:

      - id: "T01_T01"
        name: "Write tests for QuestionTUI._build_questions_table()"
        status: "pending"
        priority: "high"
        agent: "build-python"
        inputs:
          - "/home/code/AgenticEngineering/modules/AgenticCLI/src/agenticcli/tui/question_tui.py"
        target_files:
          - "/home/code/AgenticEngineering/modules/AgenticCLI/tests/test_question_tui.py"
        guidance: |
          Create test_question_tui.py in the AgenticCLI tests directory.

          Tests to cover:
          1. test_build_table_empty_plans: tmp_path has no live plans dir ->
             table has one row with "No pending questions" message
          2. test_build_table_no_pending_questions: live plan dir exists but no
             questions/pending/*.yml files -> "No pending questions" row
          3. test_build_table_one_question: one pending question YAML exists ->
             table has one data row with correct plan, id, severity, question text
          4. test_build_table_multi_plan: two plans each with one pending question ->
             table has two rows
          5. test_build_table_severity_ordering: critical question appears before low ->
             rows ordered by severity (critical, high, medium, low)

          Use pytest tmp_path for creating fake plan folder structures.
          Mock agenticguidance imports if needed (use monkeypatch or try/except skip).
          Use Rich's Table introspection to verify row count and content.

        success_criteria:
          - "test_question_tui.py exists with at least 5 test functions"
          - "All tests pass: python3 -m pytest modules/AgenticCLI/tests/test_question_tui.py -v"
          - "Tests do not import ntfy or watchdog"

      - id: "T01_T02"
        name: "Write tests for notify_question_window()"
        status: "pending"
        priority: "medium"
        agent: "build-python"
        inputs:
          - "/home/code/AgenticEngineering/modules/AgenticCLI/src/agenticcli/utils/tmux_notify.py"
        target_files:
          - "/home/code/AgenticEngineering/modules/AgenticCLI/tests/test_tmux_notify.py"
        guidance: |
          Create or extend test_tmux_notify.py.

          Tests to cover:
          1. test_notify_question_window_not_in_tmux: TMUX env var not set ->
             returns False, no subprocess call
          2. test_notify_question_window_in_tmux_success: TMUX env var set,
             subprocess.run succeeds -> returns True
          3. test_notify_question_window_in_tmux_failure: subprocess.run raises
             CalledProcessError -> returns False (graceful degradation)
          4. test_deprecated_functions_removed: importing tmux_notify should NOT
             expose notify_questions_in_tmux or clear_notification_pane

          Use monkeypatch to set os.environ["TMUX"] and mock subprocess.run.

        success_criteria:
          - "All tests pass: python3 -m pytest modules/AgenticCLI/tests/test_tmux_notify.py -v"
          - "test_deprecated_functions_removed passes (functions do not exist)"

  - id: "T02"
    name: "Regression tests for question CLI commands"
    status: "pending"
    tasks:

      - id: "T02_T01"
        name: "Verify existing question command tests still pass"
        status: "pending"
        priority: "high"
        agent: "build-python"
        inputs:
          - "/home/code/AgenticEngineering/modules/AgenticCLI/tests/"
        target_files: []
        guidance: |
          Run the existing question-related tests and verify they all pass:

            python3 -m pytest modules/AgenticCLI/tests/ -v -k "question" 2>&1

          If any tests fail due to missing ntfy imports, fix the tests by removing
          those imports (they should test the new behavior, not ntfy).

          If tests mock ntfy.send_ntfy, update mocks to mock notify_question_window
          instead.

          This is a read-and-fix task - inspect the test files, identify any ntfy
          references, and update them.

        success_criteria:
          - "All question-related tests pass with exit code 0"
          - "No test file imports from agenticcli.utils.ntfy"
          - "No test file imports from agenticcli.services.ntfy_question_agent"

  - id: "T03"
    name: "Smoke test: no ntfy references remain"
    status: "pending"
    tasks:

      - id: "T03_T01"
        name: "Codebase scan for remaining ntfy references"
        status: "pending"
        priority: "high"
        agent: "build-python"
        inputs: []
        target_files: []
        guidance: |
          Run the following commands and verify each returns no matches:

            grep -r "from agenticcli.utils.ntfy" /home/code/AgenticEngineering/modules/
            grep -r "from agenticcli.services.ntfy_question_agent" /home/code/AgenticEngineering/modules/
            grep -r "import send_ntfy" /home/code/AgenticEngineering/modules/
            grep -r "NtfyReplyPoller" /home/code/AgenticEngineering/modules/
            grep -r "NtfyQuestionAgent" /home/code/AgenticEngineering/modules/
            grep -r "ntfy_state.yml" /home/code/AgenticEngineering/modules/

          If any matches are found, fix them before marking this task complete.

        success_criteria:
          - "Zero matches for all grep commands above"
          - "No ntfy files exist under modules/AgenticCLI/src/agenticcli/"

test_strategy:
  - type: "unit"
    scope: "QuestionTUI class (table building, answer flow)"
    location: "modules/AgenticCLI/tests/test_question_tui.py"
    command: "python3 -m pytest modules/AgenticCLI/tests/test_question_tui.py -v"

  - type: "unit"
    scope: "notify_question_window() and tmux_notify helpers"
    location: "modules/AgenticCLI/tests/test_tmux_notify.py"
    command: "python3 -m pytest modules/AgenticCLI/tests/test_tmux_notify.py -v"

  - type: "regression"
    scope: "All existing question CLI command tests"
    location: "modules/AgenticCLI/tests/"
    command: "python3 -m pytest modules/AgenticCLI/tests/ -v -k 'question' 2>&1"

  - type: "smoke"
    scope: "No ntfy references in codebase"
    location: "modules/"
    command: "grep -r 'ntfy' modules/AgenticCLI/src/ && echo FAIL || echo PASS"
