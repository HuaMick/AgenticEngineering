name: "Auto apply dangerously-skip-permissions to session spawns"
worktree_path: "/home/code/AgenticEngineering-cli-nesting"
branch: "auto_skip_permissions"
status: "completed"

objective: |
  Modify `cmd_spawn` in `commands/session.py` to always pass `--dangerously-skip-permissions`
  to the background/foreground `claude` CLI commands.

success_criteria:
  - "agentic session spawn" always bypasses the Claude CLI confirmation prompts implicitly.

phases:
  - name: "Phase 1 - Implementation"
    id: "phase_01"
    status: "completed"
    tasks:
      - id: "phase_01_001"
        name: "Auto append --dangerously-skip-permissions in cmd_spawn()"
        status: "completed"
        target_files:
          - "modules/AgenticCLI/src/agenticcli/commands/session.py"
