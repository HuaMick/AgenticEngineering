plan:
  name: Test Phase - Fix Task ID Type Mismatch
  worktree: /home/code/AgenticEngineering-260202TF
  branch: 260202TF
  status: pending
  created: '2026-02-02'
  objective: |
    Validate that task ID type mismatch bug is fixed and all task commands
    work correctly with various task ID formats.
  success_criteria:
    - All unit tests pass
    - Integration tests verify mixed ID formats work
    - Regression tests confirm existing plans still work
    - Manual verification tests succeed
  scope:
    includes:
      - modules/AgenticCLI/tests/
      - docs/plans/live/
    excludes: []
  phases:
    - id: T1
      name: Automated Testing
      status: pending
      tasks:
        - id: '01'
          name: Run unit tests
          description: |
            Execute unit tests for task ID normalization:
            ```bash
            cd /home/code/AgenticEngineering-260202TF
            pytest modules/AgenticCLI/tests/test_task_id_handling.py -v
            ```

            Expected: All tests pass, showing normalization works.
          status: pending
        - id: '02'
          name: Run integration tests
          description: |
            Execute integration tests with test plan folder:
            ```bash
            pytest modules/AgenticCLI/tests/integration/test_task_commands.py -v
            ```

            Expected: All task commands work with various ID formats.
          status: pending
        - id: '03'
          name: Run full CLI test suite
          description: |
            Run complete CLI test suite to ensure no regressions:
            ```bash
            pytest modules/AgenticCLI/tests/ -v
            ```

            Expected: All tests pass, no new failures.
          status: pending
    - id: T2
      name: Manual Verification
      status: pending
      tasks:
        - id: '01'
          name: Test with decimal task IDs
          description: |
            Create test plan with decimal IDs and verify:
            ```bash
            # List tasks
            agentic plan task list --plan test_plan -j

            # Start task with decimal ID
            agentic plan task start 01.1 --plan test_plan

            # Check status
            agentic plan task status 01.1 --plan test_plan

            # Complete task
            agentic plan task complete 01.1 --plan test_plan
            ```

            Expected: All commands work without "Task not found" error.
          status: pending
        - id: '02'
          name: Test with existing live plans
          description: |
            Verify existing plans still work:
            ```bash
            # List tasks from real plan
            agentic plan task list --plan 260201TE_typer_cli_test_suite

            # Try to start a task (dry-run)
            agentic plan task status 01 --plan 260201TE_typer_cli_test_suite
            ```

            Expected: Commands work with existing task IDs.
          status: pending
        - id: '03'
          name: Test edge cases
          description: |
            Test edge cases:
            - Task ID with leading zeros: 001
            - Task ID as pure float: 1.1
            - Task ID as string float: '1.1'
            - Task ID with quotes: '01.1'
            - Whitespace around ID: ' 01 '

            Expected: All formats are handled correctly.
          status: pending
    - id: T3
      name: Validation & Sign-off
      status: pending
      tasks:
        - id: '01'
          name: Review test coverage
          description: |
            Verify test coverage for task ID handling:
            ```bash
            pytest --cov=agenticcli.commands.plan --cov-report=term-missing
            ```

            Expected: Task ID comparison code has 100% coverage.
          status: pending
        - id: '02'
          name: Document test results
          description: |
            Create test report documenting:
            - All test cases executed
            - Pass/fail status
            - Any edge cases discovered
            - Performance impact (if any)

            Output: docs/plans/live/260202TF_fix_task_id_type_mismatch/test_report.md
          status: pending
        - id: '03'
          name: User acceptance criteria verification
          description: |
            Confirm all acceptance criteria from bug report are met:
            - Task ID 01.1 can be found
            - Task ID 1.1 can be found
            - String and numeric IDs work correctly
            - No regression in existing functionality

            Expected: All criteria satisfied.
          status: pending
