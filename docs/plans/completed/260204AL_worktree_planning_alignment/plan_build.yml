name: worktree-planning-alignment
worktree_path: /home/code/AgenticEngineering
branch: feature/worktree-planning-alignment
status: completed
priority: high
context: 'The current implementation violates the fundamental 1:1 relationship between
  git worktrees

  and planning folders. All live plans are incorrectly assigned to main branch. Need
  to add

  CLI worktree commands for standardized worktree creation and enforce branch requirements

  in plan creation.

  '
objective: 'Add `agentic worktree` CLI commands for standardized worktree management
  with automatic

  workspace file updates. Enforce 1:1 relationship between worktrees and live planning
  folders

  by requiring --branch parameter in plan creation. Add validation to prevent multiple
  live

  plans per worktree.

  '
phases:
- name: 'Phase 1: CLI Worktree Commands'
  execution: sequential
  tasks:
  - id: AL_001
    name: Create worktree.py command module
    description: 'Create new CLI command module for worktree management.

      Implement: create, list, status, remove subcommands.

      '
    status: completed
    target_files:
    - modules/AgenticCLI/src/agenticcli/commands/worktree.py
    guidance: '- Use Typer for command structure (consistent with other commands)

      - worktree create <branch-name> --base <base>: Create new worktree

      - worktree list: List all worktrees with status

      - worktree status: Current worktree context

      - worktree remove <branch-name>: Safe worktree removal

      '
  - id: AL_002
    name: Implement workspace file auto-update
    description: 'Add logic to automatically update .code-workspace file when worktrees

      are created or removed via CLI.

      '
    status: completed
    target_files:
    - modules/AgenticCLI/src/agenticcli/commands/worktree.py
    guidance: '- On create: Add entry to folders[] and git.scanRepositories[]

      - On remove: Remove entry from both arrays

      - Detect workspace file location from main worktree

      - Handle case where workspace file doesn''t exist (skip with warning)

      '
  - id: AL_003
    name: Register worktree commands in CLI
    description: 'Register the new worktree command group in the main CLI entry point.

      '
    status: completed
    target_files:
    - modules/AgenticCLI/src/agenticcli/cli.py
- name: 'Phase 2: Plan Command Enforcement'
  execution: sequential
  tasks:
  - id: AL_004
    name: Add required --branch parameter to plan create
    description: 'Modify plan create command to require --branch parameter.

      Validate branch exists as worktree before allowing plan creation.

      '
    status: completed
    target_files:
    - modules/AgenticCLI/src/agenticcli/commands/plan.py
    guidance: '- Add required --branch parameter

      - Error if branch is not a valid worktree

      - Error if worktree already has a live planning folder

      '
  - id: AL_005
    name: Add 1:1 relationship validation
    description: 'Add validation logic to enforce max 1 live plan per worktree branch

      and prevent live plans on main branch.

      '
    status: completed
    target_files:
    - modules/AgenticGuidance/src/agenticguidance/services/plan.py
    guidance: '- Check if worktree already has live planning folder

      - Prevent multiple live plans for same worktree

      - Prevent live plans on main branch

      '
- name: 'Phase 3: Tests and Validation'
  execution: sequential
  tasks:
  - id: AL_006
    name: Create worktree command tests
    description: 'Create tests for worktree CLI commands covering create, list, status,
      remove.

      '
    status: completed
    target_files:
    - modules/AgenticCLI/tests/test_worktree.py
    notes: 'Tests exist in test_worktree.py - 29 tests pass covering helpers, list, create, remove, status commands.'
  - id: AL_007
    name: Create plan enforcement tests
    description: 'Create tests for --branch requirement and 1:1 relationship validation.

      '
    status: completed
    target_files:
    - modules/AgenticCLI/tests/test_plan_init.py
    notes: 'Tests exist in test_plan_init.py - 14 tests pass including TestPlanInitEnforcement class.'
  - id: AL_008
    name: Run full test suite
    description: Run pytest to validate all changes work correctly.
    status: completed
    notes: 'All 43 tests for worktree and plan_init pass. Pre-existing failures in unrelated modules (state.py, task_workflow.py, template_workflow.py) are out of scope.'
success_criteria:
- agentic worktree create command exists and works
- agentic worktree list shows all worktrees
- agentic plan create REQUIRES --branch parameter
- Workspace file automatically updated on worktree create/remove
- Each worktree branch can have MAX 1 live planning folder
- Tests pass for all new functionality
