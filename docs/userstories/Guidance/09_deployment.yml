area: deployment
description: "Worktree, CI/CD, build/packaging, environment"
stories:
- id: US-GDN-084
  title: Create Git Worktree for Feature Branch
  category: deployment
  priority: critical
  description: As a deploy-worktree agent, I want to create git worktree for feature branch development, so that code development
    is isolated from main branch.
  prerequisites: []
  steps:
  - step: 1
    when: the feature is used
    then:
    - Create worktree at specified path
  - step: 2
    when: the feature is used
    then:
    - Create and checkout feature branch
  - step: 3
    when: the feature is used
    then:
    - Verify worktree creation successful
  - step: 4
    when: the feature is used
    then:
    - Initialize VS Code workspace if requested
  - step: 5
    when: the feature is used
    then:
    - Handle existing worktree gracefully
  related_commands:
  - git worktree add <path> -b <branch>
  related_agents:
  - deploy-worktree
  related_files:
  - modules/AgenticGuidance/agents/deploy/deploy-worktree/process.yml
  last_tested: null
  test_status: untested
  test_notes: ''
  tested_by_plan: null
- id: US-GDN-085
  title: Initialize Plan Folder Structure
  category: deployment
  priority: critical
  description: As a deploy-worktree agent, I want to create epic folder structure in docs/epics/live/, so that ticket files
    have consistent organization.
  prerequisites: []
  steps:
  - step: 1
    when: the feature is used
    then:
    - 'Create epic folder: docs/epics/live/YYMMDDXX_description/'
  - step: 2
    when: the feature is used
    then:
    - 'Create subdirectories: analysis/, reference/, questions/'
  - step: 3
    when: the feature is used
    then:
    - Create initial README.md with epic metadata
  - step: 4
    when: the feature is used
    then:
    - 'Verify folder naming convention: YYMMDDXX_description'
  - step: 5
    when: the feature is used
    then:
    - Handle existing folder gracefully
  related_commands:
  - agentic epic init <branch> --description <desc>
  related_agents:
  - deploy-worktree
  last_tested: null
  test_status: untested
  test_notes: ''
  tested_by_plan: null
- id: US-GDN-086
  title: VS Code Workspace Configuration
  category: deployment
  priority: medium
  description: As a deploy-worktree agent, I want to create VS Code workspace for feature development, so that developers
    have IDE configured for worktree.
  prerequisites: []
  steps:
  - step: 1
    when: the feature is used
    then:
    - Create .code-workspace file with worktree path
  - step: 2
    when: the feature is used
    then:
    - Configure workspace settings for project
  - step: 3
    when: the feature is used
    then:
    - Configure recommended extensions
  - step: 4
    when: the feature is used
    then:
    - Configure Python interpreter path
  - step: 5
    when: the feature is used
    then:
    - Open workspace in VS Code if requested
  related_agents:
  - deploy-worktree
  last_tested: null
  test_status: untested
  test_notes: ''
  tested_by_plan: null
- id: US-GDN-087
  title: Worktree Cleanup and Removal
  category: deployment
  priority: high
  description: As a deploy-worktree agent, I want to remove worktree after feature completion, so that worktrees don't accumulate
    unnecessarily.
  prerequisites: []
  steps:
  - step: 1
    when: the feature is used
    then:
    - Verify branch merged to main before removal
  - step: 2
    when: the feature is used
    then:
    - Check for uncommitted changes before removal
  - step: 3
    when: the feature is used
    then:
    - Remove worktree using git worktree remove
  - step: 4
    when: the feature is used
    then:
    - Clean up VS Code workspace file
  - step: 5
    when: the feature is used
    then:
    - Handle active worktree sessions gracefully
  related_commands:
  - git worktree remove <path>
  related_agents:
  - deploy-worktree
  last_tested: null
  test_status: untested
  test_notes: ''
  tested_by_plan: null
- id: US-GDN-088
  title: Main-First Planning Workflow
  category: deployment
  priority: critical
  description: As a deploy-worktree agent, I want to support main-first planning workflow, so that plans are visible in main
    worktree before execution.
  prerequisites: []
  steps:
  - step: 1
    when: the feature is used
    then:
    - Create epic folder in main worktree docs/epics/live/
  - step: 2
    when: the feature is used
    then:
    - Create feature worktree for code implementation
  - step: 3
    when: the feature is used
    then:
    - Plan visible from main worktree for all developers
  - step: 4
    when: the feature is used
    then:
    - Code development happens in feature worktree
  - step: 5
    when: the feature is used
    then:
    - Merge via staging to main after completion
  related_agents:
  - deploy-worktree
  last_tested: null
  test_status: untested
  test_notes: ''
  tested_by_plan: null
- id: US-GDN-089
  title: CI/CD Pipeline Configuration
  category: deployment
  priority: medium
  description: As a deploy-cicd agent, I want to configure CI/CD pipeline for automated testing, so that code changes are
    validated before merge.
  prerequisites: []
  steps:
  - step: 1
    when: the feature is used
    then:
    - Create cloudbuild.yaml for Google Cloud Build
  - step: 2
    when: the feature is used
    then:
    - Create Dockerfile.test for test environment
  - step: 3
    when: the feature is used
    then:
    - Create GitHub Actions workflow if applicable
  - step: 4
    when: the feature is used
    then:
    - Configure test execution steps
  - step: 5
    when: the feature is used
    then:
    - Configure deployment steps
  related_agents:
  - deploy-cicd
  related_files:
  - modules/AgenticGuidance/agents/deploy/deploy-cicd/process.yml
  last_tested: null
  test_status: untested
  test_notes: ''
  tested_by_plan: null
- id: US-GDN-090
  title: CI/CD Infrastructure Status Check
  category: deployment
  priority: critical
  description: As a planner-reviewer agent, I want to verify CI/CD infrastructure exists before planning CI/CD tasks, so that
    plans don't reference unimplemented infrastructure.
  prerequisites: []
  steps:
  - step: 1
    when: the feature is used
    then:
    - Check AgenticGuidance README Implementation Status
  - step: 2
    when: the feature is used
    then:
    - Verify deploy-cicd status is 'Implemented' not 'Guidance Only'
  - step: 3
    when: the feature is used
    then:
    - Verify cloudbuild.yaml exists in module
  - step: 4
    when: the feature is used
    then:
    - Verify Dockerfile.test exists in module
  - step: 5
    when: the feature is used
    then:
    - Block plans requiring CI/CD if infrastructure not ready
  related_agents:
  - planner-reviewer
  - deploy-cicd
  related_files:
  - modules/AgenticGuidance/README.md
  last_tested: null
  test_status: untested
  test_notes: ''
  tested_by_plan: null
- id: US-GDN-091
  title: CI/CD Validation Phase
  category: deployment
  priority: medium
  description: As a deploy-cicd agent, I want to validate CI/CD configuration before deployment, so that pipeline configuration
    is correct.
  prerequisites: []
  steps:
  - step: 1
    when: the feature is used
    then:
    - Validate cloudbuild.yaml syntax
  - step: 2
    when: the feature is used
    then:
    - Validate Dockerfile.test builds successfully
  - step: 3
    when: the feature is used
    then:
    - Validate test commands execute correctly
  - step: 4
    when: the feature is used
    then:
    - Run smoke tests in CI environment
  - step: 5
    when: the feature is used
    then:
    - Report validation results
  related_agents:
  - deploy-cicd
  last_tested: null
  test_status: untested
  test_notes: ''
  tested_by_plan: null
- id: US-GDN-092
  title: Python Build and Packaging
  category: deployment
  priority: high
  description: As a build-python agent, I want to build Python packages for backend and CLI, so that code is packaged for
    distribution.
  prerequisites: []
  steps:
  - step: 1
    when: the feature is used
    then:
    - Build Python package using pyproject.toml
  - step: 2
    when: the feature is used
    then:
    - Run pytest test suite
  - step: 3
    when: the feature is used
    then:
    - Check code quality with ruff
  - step: 4
    when: the feature is used
    then:
    - Build wheel and sdist distributions
  - step: 5
    when: the feature is used
    then:
    - Verify package metadata
  related_commands:
  - python3 -m build
  - pytest
  related_agents:
  - build-python
  related_files:
  - modules/AgenticGuidance/agents/build/build-python/process.yml
  last_tested: null
  test_status: untested
  test_notes: ''
  tested_by_plan: null
- id: US-GDN-093
  title: Python Dependency Management
  category: deployment
  priority: high
  description: As a build-python agent, I want to manage Python dependencies correctly, so that builds are reproducible.
  prerequisites: []
  steps:
  - step: 1
    when: the feature is used
    then:
    - Verify pyproject.toml dependencies
  - step: 2
    when: the feature is used
    then:
    - Create or update requirements.txt if needed
  - step: 3
    when: the feature is used
    then:
    - Check for dependency conflicts
  - step: 4
    when: the feature is used
    then:
    - Pin versions for reproducibility
  - step: 5
    when: the feature is used
    then:
    - Document dependency rationale
  related_agents:
  - build-python
  last_tested: null
  test_status: untested
  test_notes: ''
  tested_by_plan: null
- id: US-GDN-094
  title: Python Installation Verification
  category: deployment
  priority: medium
  description: As a build-python agent, I want to verify package installs correctly, so that users can install and use the
    package.
  prerequisites: []
  steps:
  - step: 1
    when: the feature is used
    then:
    - Install package in clean virtual environment
  - step: 2
    when: the feature is used
    then:
    - Verify CLI commands are on PATH
  - step: 3
    when: the feature is used
    then:
    - Run smoke tests against installed package
  - step: 4
    when: the feature is used
    then:
    - Verify all modules importable
  - step: 5
    when: the feature is used
    then:
    - Report installation issues
  related_agents:
  - build-python
  last_tested: null
  test_status: untested
  test_notes: ''
  tested_by_plan: null
- id: US-GDN-095
  title: Flutter Build for Mobile and Web
  category: deployment
  priority: medium
  description: As a build-flutter agent, I want to build Flutter applications for target platforms, so that frontend is packaged
    for distribution.
  prerequisites: []
  steps:
  - step: 1
    when: the feature is used
    then:
    - Build Flutter app for specified platform (iOS, Android, web)
  - step: 2
    when: the feature is used
    then:
    - Run Flutter test suite
  - step: 3
    when: the feature is used
    then:
    - Check code quality with flutter analyze
  - step: 4
    when: the feature is used
    then:
    - Build release artifacts
  - step: 5
    when: the feature is used
    then:
    - Verify build artifacts
  related_commands:
  - flutter build <platform>
  - flutter test
  related_agents:
  - build-flutter
  related_files:
  - modules/AgenticGuidance/agents/build/build-flutter/process.yml
  last_tested: null
  test_status: untested
  test_notes: ''
  tested_by_plan: null
- id: US-GDN-096
  title: Flutter Dependency Management
  category: deployment
  priority: medium
  description: As a build-flutter agent, I want to manage Flutter dependencies correctly, so that builds are reproducible.
  prerequisites: []
  steps:
  - step: 1
    when: the feature is used
    then:
    - Verify pubspec.yaml dependencies
  - step: 2
    when: the feature is used
    then:
    - Run flutter pub get
  - step: 3
    when: the feature is used
    then:
    - Check for dependency conflicts
  - step: 4
    when: the feature is used
    then:
    - Pin versions for reproducibility
  - step: 5
    when: the feature is used
    then:
    - Document dependency rationale
  related_agents:
  - build-flutter
  last_tested: null
  test_status: untested
  test_notes: ''
  tested_by_plan: null
- id: US-GDN-097
  title: Build Phase in Orchestration
  category: deployment
  priority: high
  description: As a orchestration-executor agent, I want to execute build phases using appropriate build agents, so that code
    is compiled and packaged correctly.
  prerequisites: []
  steps:
  - step: 1
    when: the feature is used
    then:
    - Route Python build tasks to build-python agent
  - step: 2
    when: the feature is used
    then:
    - Route Flutter build tasks to build-flutter agent
  - step: 3
    when: the feature is used
    then:
    - Execute build phases in dependency order
  - step: 4
    when: the feature is used
    then:
    - Validate build artifacts before proceeding
  - step: 5
    when: an error condition occurs
    then:
    - Handle build failures gracefully
  related_agents:
  - orchestration-executor
  - build-python
  - build-flutter
  last_tested: null
  test_status: untested
  test_notes: ''
  tested_by_plan: null
- id: US-GDN-098
  title: Deploy Phase in Orchestration
  category: deployment
  priority: medium
  description: As a orchestration-executor agent, I want to execute deploy phases using deploy agents, so that infrastructure
    is configured correctly.
  prerequisites: []
  steps:
  - step: 1
    when: the feature is used
    then:
    - Route worktree tasks to deploy-worktree agent
  - step: 2
    when: the feature is used
    then:
    - Route CI/CD tasks to deploy-cicd agent (if implemented)
  - step: 3
    when: the feature is used
    then:
    - Execute deploy phases before build phases
  - step: 4
    when: the feature is used
    then:
    - Validate infrastructure before code execution
  - step: 5
    when: an error condition occurs
    then:
    - Handle deploy failures gracefully
  related_agents:
  - orchestration-executor
  - deploy-worktree
  - deploy-cicd
  last_tested: null
  test_status: untested
  test_notes: ''
  tested_by_plan: null
- id: US-GDN-099
  title: Absolute Paths in Configuration
  category: deployment
  priority: high
  description: As a build or deploy agent, I want to use absolute paths in configuration files, so that work-from-anywhere
    is supported.
  prerequisites: []
  steps:
  - step: 1
    when: the feature is used
    then:
    - All file paths in ticket files are absolute
  - step: 2
    when: the feature is used
    then:
    - Worktree paths are absolute
  - step: 3
    when: the feature is used
    then:
    - Config files use absolute paths
  - step: 4
    when: the feature is used
    then:
    - Agents verify path absoluteness before use
  - step: 5
    when: the feature is used
    then:
    - Relative paths rejected or converted to absolute
  related_agents:
  - build-python
  - build-flutter
  - deploy-worktree
  last_tested: null
  test_status: untested
  test_notes: ''
  tested_by_plan: null
- id: US-GDN-100
  title: Git Worktree and Branching Guidelines
  category: deployment
  priority: high
  description: As a deploy-worktree agent, I want to follow git worktree and branching guidelines, so that git workflow is
    consistent across team.
  prerequisites: []
  steps:
  - step: 1
    when: the feature is used
    then:
    - Feature branches created from main
  - step: 2
    when: the feature is used
    then:
    - Worktrees isolated by feature
  - step: 3
    when: the feature is used
    then:
    - Merge via staging branch to main
  - step: 4
    when: the feature is used
    then:
    - Clean up worktrees after merge
  - step: 5
    when: the feature is used
    then:
    - Follow naming conventions for branches
  related_agents:
  - deploy-worktree
  related_files:
  - modules/AgenticGuidance/assets/guidelines/worktree-and-branching.yml
  last_tested: null
  test_status: untested
  test_notes: ''
  tested_by_plan: null
- id: US-GDN-101
  title: Environment Validation
  category: deployment
  priority: medium
  description: As a build or deploy agent, I want to validate environment before build or deploy, so that missing dependencies
    are caught early.
  prerequisites: []
  steps:
  - step: 1
    when: the feature is used
    then:
    - Check required tools installed (python3, git, flutter, etc.)
  - step: 2
    when: the feature is used
    then:
    - Check tool versions meet requirements
  - step: 3
    when: the feature is used
    then:
    - Check required environment variables set
  - step: 4
    when: an error condition occurs
    then:
    - Report missing or incorrect dependencies
  - step: 5
    when: the feature is used
    then:
    - Suggest installation commands if possible
  related_agents:
  - build-python
  - build-flutter
  - deploy-worktree
  - deploy-cicd
  last_tested: null
  test_status: untested
  test_notes: ''
  tested_by_plan: null
- id: US-GDN-102
  title: DevOps Pipeline Audit
  category: deployment
  priority: medium
  description: As a deploy-cicd agent, I want to audit the DevOps pipeline configuration, so that missing or incomplete CI/CD
    configurations are identified and reported.
  prerequisites: []
  steps:
  - step: 1
    when: the feature is used
    then:
    - agentic devops commands provide worktree and build tooling
  - step: 2
    when: the feature is used
    then:
    - Audit reports missing/incomplete configurations
  - step: 3
    when: the feature is used
    then:
    - CI/CD pipeline for AgenticGuidance not yet implemented
  related_agents:
  - deploy-cicd
  last_tested: null
  test_status: untested
  test_notes: ''
  tested_by_plan: null
