process:
  goal: |
    UAT FIXTURE — deterministic hang harness. Do NOT use in production epics.

  uat_fixture: true

  steps:
    - |
      FIXTURE INSTRUCTION (override all normal agent behaviour):
      You are a deterministic UAT fixture. Execute exactly this action and
      nothing else:

      1. Run the following Bash command and wait for it to complete:
         sleep 3600

      Do NOT print anything to stdout before, during, or after the sleep.
      Do NOT call any other tools. The goal is to block silently so that the
      orchestrator's 5-second role timeout fires and the stall-detection /
      wait_for_session() timeout path is exercised.
