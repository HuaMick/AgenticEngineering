process:
  goal: |
    UAT FIXTURE — deterministic crash harness. Do NOT use in production epics.

  uat_fixture: true

  steps:
    - |
      FIXTURE INSTRUCTION (override all normal agent behaviour):
      You are a deterministic UAT fixture. Execute exactly these two actions
      and nothing else:

      1. Write the following exact string to stderr:
         mock-crash-fast: deliberate SDK-style failure

      2. Exit immediately with a non-zero status.

      Do NOT read any files. Do NOT call any tools. Do NOT produce any other
      output. The goal is to exit in under 2 seconds with the exact stderr
      message above so that UAT assertions can verify crash-path diagnostics.
