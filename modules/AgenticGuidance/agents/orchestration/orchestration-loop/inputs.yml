# Orchestration Loop Inputs
# Configuration for the full-lifecycle orchestration loop.

inputs:
  max_loop_iterations:
    type: "integer"
    default: 10
    description: "Maximum number of full discovery-plan-execute cycles"

  completion_promise:
    type: "string"
    default: "All live plans completed or blocked. Loop finished."
    description: "Promise text emitted when loop completes"

  parallelism:
    guidance_plans:
      description: "Guidance-only plans (teacher-update-guidance): safe to run in parallel"
      parallel: true
    build_plans:
      description: "CLI build plans (build-python): run sequentially (shared files)"
      parallel: false

  cli_error_recovery:
    reference: "modules/AgenticGuidance/assets/guidelines/cli-error-recovery.yml"
    description: "DOGFOOD RULE - mandatory CLI error recovery protocol"
