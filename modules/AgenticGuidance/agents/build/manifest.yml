# Build Agent Family Manifest
# Category: Build
# Purpose: Building and compiling code for production deployment

manifest:
  version: "1.0"
  agent_family: "build"

category:
  definition: "modules/AgenticGuidance/assets/definitions/agent-categories.yml#build"

sub_agents:
  - name: "build-flutter"
    path: "build-flutter/"
    description: "Flutter-specific build agent for mobile/web frontend components"

  - name: "build-python"
    path: "build-python/"
    description: "Python-specific build agent for backend services and CLI components"
