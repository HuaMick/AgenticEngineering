# ==============================================
# PATH RESOLUTION SEMANTICS
# ==============================================
# All paths in this file are relative to the REPOSITORY ROOT
# (e.g., modules/AgenticGuidance/assets/), NOT the target project root.
#
# EXCEPTION: docs/epics/ paths are relative to the TARGET PROJECT root.
# When an agent references docs/epics/live/ or docs/epics/completed/,
# these exist in the project being worked on, not in AgenticGuidance.
# ==============================================

# ==============================================
# REQUIRED INPUTS
# ==============================================
# These inputs MUST be provided by the parent orchestrator or user.
# Without these, the agent cannot perform the epic folder audit.
# ==============================================
required_inputs:
  - name: "epic_folder_path"
    type: "string"
    description: "Path to the epic folder to audit"
    required: true
    example: "docs/epics/live/260106MyProject_feature_auth/"

  - name: "target_project_path"
    type: "string"
    description: "Absolute path to target project root (for docs/epics/ resolution)"
    required: true
    example: "/home/code/MyProject"

# Planner-Audit Agent Inputs
# Defines the files, directories, and context needed to audit epic folder compliance.
# See process.yml for goal, execution steps, and output structure.

agent_context:
  category: "modules/AgenticGuidance/assets/definitions/agent-categories.yml#agent_categories.planner"
  role: "planner-audit"

inputs:
  agent_type: "planner-audit"
  version: "2.0"  # MUST match manifest.yml version

  # Reference layers (transitive loading)
  # NOTE: Uses planner-core layers for shared definitions
  layers:
    - type: layer
      path: "modules/AgenticGuidance/assets/inputs/planner-core-system.yml"
      description: "Planner-specific system definitions (plans.yml - authoritative lifecycle rules)"
      required: true

    - type: layer
      path: "modules/AgenticGuidance/assets/inputs/planner-core-guidelines.yml"
      description: "Planner-specific guidelines (context-minimisation, etc.)"
      required: true

    - type: layer
      path: "modules/AgenticGuidance/assets/inputs/planner-shared.yml"
      description: "Planner shared inputs (folder-structure, iteration patterns)"
      required: true

  # Audit-specific inputs
  core_inputs:
    # Plan lifecycle and file taxonomy (CRITICAL for audit rules)
    - type: file
      path: "modules/AgenticGuidance/assets/definitions/plans.yml"
      description: "Authoritative plan lifecycle, file taxonomy, and completion patterns"
      required: true

    - type: file
      path: "modules/AgenticGuidance/assets/guidelines/plan-completion.yml"
      description: "Copy-and-Sync pattern details for understanding file placement rules"
      required: true

    # Scrap file handling
    - type: file
      path: "modules/AgenticGuidance/assets/definitions/plans.yml#scrap_file_taxonomy"
      description: "Lifecycle rules for audit, review, and remediation files"
      required: true

    # Folder structure reference
    - type: file
      path: "modules/AgenticGuidance/assets/definitions/plans.yml#path_conventions"
      description: "Path conventions and folder structure for epic folders"
      required: false

    # The target epic folder to audit
    - type: pattern
      path: "docs/epics/live/YYMMDDXX_description/"
      description: "The epic folder to audit. Contains live/, completed/, audit/, analysis/ subfolders."
      required: true

    # Example plan for reference
    - type: file
      path: "modules/AgenticGuidance/assets/examples/planner/YYMMDDXX_description/plan_example.yml"
      description: "Reference plan showing expected structure and status fields"
      required: true

  # Audit-specific guidelines
  guidelines:
    - type: file
      path: "modules/AgenticGuidance/assets/guidelines/decommissioning-over-deprecation.yml"
      description: "Prefer complete removal over soft deprecation - applies to stale plan artifacts"
      required: true

  # Audit-specific definitions
  # See plans.yml for authoritative file placement rules and lifecycle
  definitions:
    compliance_question: |
      For each file: "Is this file compliant? Does it belong here?"
      Files that fail create friction and potential misdirection.

    file_placement_rules: |
      See plans.yml#path_conventions for authoritative rules.
      live/: Active work. completed/: Finished work. audit/: Audit reports. analysis/: Iteration history.
      Epic folders live under docs/epics/live/YYMMDDXX_description/.

    misleading_content_patterns: |
      Content that could mislead: references to deleted files, outdated recommendations,
      unresolved TODOs for resolved issues, superseded plans without documentation.

    execution_tracking: |
      Verify: plans marked complete have execution evidence, active plans show activity,
      abandoned plans are documented, partial completion is indicated.
