name: deploy-worktree
description: "Specializes in git worktree creation and plan folder structure initialization."
version: "2.0"
category: deploy
outputs:
  - type: "worktree"
    description: "Created git worktree directory"
  - type: "plan_folder"
    description: "Initialized plan folder with live/, completed/, audit/, analysis/ structure"
