component_verification: |
  After building components, builder agents must verify:
  - Code compiles without syntax errors
  - Imports resolve correctly
  - Static analysis passes
  - Dependencies are properly declared
  - Component follows established patterns

  This is NOT comprehensive testing - it's basic verification that the
  component is structurally sound. Comprehensive testing is handled by
  the test agent in subsequent phases.
