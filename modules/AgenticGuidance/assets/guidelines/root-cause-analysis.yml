root-cause-analysis: |
    Fix the source, not the symptom.

    Pattern:
    - Reproduce → identify the earliest failing signal → trace to cause → fix → re-run
    - Validate tests are correct before changing production code


