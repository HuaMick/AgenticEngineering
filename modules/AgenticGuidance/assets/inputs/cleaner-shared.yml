# Layer 3: Cleaner Shared
# Specialized guidelines and configurations for cleaner agents and cleanup operations
# Used by: 6 cleaner agents (cleaner-core, cleaner-directories, cleaner-gitignore, cleaner-old-branches, cleaner-old-tests, cleaner-unused-files)

layer_name: "cleaner-shared"
description: "Shared guidelines, voting systems, and preservation rules for all cleaner agents performing cleanup and maintenance operations"
version: "1.0"

references:
  - path: "modules/AgenticGuidance/assets/definitions/cleaner-shared-guidelines.yml"
    description: "Common guidelines for all cleaner operations"
    required: true

  - path: "modules/AgenticGuidance/assets/definitions/agent-loops.yml#cleaner-dependency-loop"
    description: "Cleaner decision-making via dependency checking loop"
    required: true

  # NOTE: cleaner-core agent not yet implemented
  # - path: "modules/AgenticGuidance/agents/cleaner/cleaner-core/manifest.yml"
  #   description: "Cleaner core agent manifest and configuration"
  #   required: true

  - path: "modules/AgenticGuidance/assets/guidelines/preserved-files.yml"
    description: "Files that must never be deleted or modified"
    required: true

  - path: "modules/AgenticGuidance/assets/examples/cleaner/dir-allowed.yml"
    description: "Example of allowed directory patterns"
    required: true

  - path: "modules/AgenticGuidance/assets/examples/cleaner/dir-not-allowed.yml"
    description: "Example of disallowed directory patterns"
    required: true

  - path: "modules/AgenticGuidance/assets/definitions/overengineering.yml"
    description: "Overengineering detection and prevention"
    required: true

  - path: "modules/AgenticGuidance/assets/definitions/acceptable-skips.yml"
    description: "Guidelines for handling skipped tests during cleanup"
    required: true
