# Layer 1: Core System
# Foundational system definitions that establish the structure and flow of all agent operations
# Used by: 27 agents across all categories (planner, builder, tester, cleaner, deploy, explore, teacher)

layer_name: "core-system"
description: "Core system architecture definitions including plans, domains, workflows, and entrypoints that form the foundation of agent operations"
version: "1.0"

references:
  - path: "modules/AgenticGuidance/assets/definitions/plans.yml"
    description: "Plan structure and lifecycle definitions"
    required: true

  - path: "modules/AgenticGuidance/assets/definitions/architecture-pattern.yml"
    description: "Consolidated architecture pattern: domains, workflows, entrypoints, and their relationships"
    required: true

  - path: "modules/AgenticGuidance/docs/ARCHITECTURE.md"
    description: "Overall system architecture documentation"
    required: true
