# Layer: Reviewer Core Guidelines
# Minimal guideline set for plan reviewers
#
# PURPOSE:
# Reviewers validate structure and completeness, not philosophical approaches.
# This layer excludes philosophical guidelines and operational concerns.
#
# RATIONALE (T3.1 from plan_guidance_reviewer_context.yml):
# Reviewers validate plan STRUCTURE, not execution approach.
# Operational guidelines belong in execution agent inputs.
#
# INCLUDED:
# - plan-completion.yml: Understanding plan lifecycle for validation
#
# EXCLUDED (with justification):
# - fix-the-source.yml: Philosophical guidance for plan creators, not reviewers
# - context-minimisation.yml: Philosophical guidance for plan creators, not reviewers
# - experiment-first.yml: Discovery guidance for plan creators, not reviewers
# - less-is-more.yml: Design philosophy for plan creators, not reviewers
# - worktree-and-branching.yml: Operational execution guidance, not plan structure review
# - response-audit.yml: Final output formatting, not plan validation

layer_name: "reviewer-core-guidelines"
description: "Minimal guidelines for plan reviewers - structure validation only"
version: "1.1"

references:
  - path: "modules/AgenticGuidance/assets/guidelines/plan-completion.yml"
    description: "Understanding plan file lifecycle for validation"
    required: true
