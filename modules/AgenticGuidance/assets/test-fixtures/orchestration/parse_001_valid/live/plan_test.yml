# Test Plan: PARSE_001 - Valid MMD with All Metadata
# Purpose: Verify executor correctly parses complete Plan-MMD
# Expected: All metadata extracted, phase execution starts

name: parse-001-valid-mmd
status: pending
priority: high
phase_type: test

context: |
  Test fixture for validating orchestration-executor MMD parsing.
  This plan contains all required metadata and should parse successfully.

phases:
  - name: "Setup_Phase"
    status: pending
    description: "Initialize test environment"
    agent_type: "deployer"
    tasks:
      - id: "setup_001"
        name: "Spawn deploy-worktree"
        status: "pending"

  - name: "Build_Phase"
    status: pending
    description: "Execute build operations"
    agent_type: "builder"
    tasks:
      - id: "build_001"
        name: "Spawn build-python"
        status: "pending"

  - name: "Test_Phase"
    status: pending
    description: "Execute test operations"
    agent_type: "tester"
    tasks:
      - id: "test_001"
        name: "Spawn test-runner"
        status: "pending"

success_criteria:
  - "All phases extracted from MMD"
  - "AGENT_ROUTING correctly parsed"
  - "FEEDBACK_TRIGGERs detected"
  - "STATUS values loaded"

expected_behavior: |
  Executor should:
  1. Parse all metadata comments successfully
  2. Build phase-to-agent routing map
  3. Initialize state from STATUS comment
  4. Begin phase execution loop
