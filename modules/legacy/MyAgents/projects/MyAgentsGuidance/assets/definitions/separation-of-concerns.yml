separation-of-concerns: |
    Do not combine conflicting incentives in one agent.

    Example:
    - Wrong: Builder changes code and declares validation complete
    - Right: Builder implements; test/audit validates independently


