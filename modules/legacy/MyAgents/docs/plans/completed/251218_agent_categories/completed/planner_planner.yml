# Planner Task: Planner Agent Category Definition
# Plan: 251218_agent_categories

task:
  agent: "planner"
  target_path: "planner/"

  objective: "Introduce agent category concept to planner agent family"

  context: |
    The planner category is responsible for planning and task decomposition across domains.
    It breaks down complex requests into actionable task plans. This task adds explicit
    category references to the planner agent family.

  current_structure:
    sub_agents:
      - "planner-build"
      - "planner-cleaning"
      - "planner-test"
      - "planner-teach"
      - "planner-agent-exam"
    has_manifest: false

  tasks:
    - id: "planner-cat-01"
      description: "Create planner/manifest.yml with category reference"
      file: "planner/manifest.yml"
      criteria: "manifest.yml created and references assets/definitions/agent-categories.yml#planner"
      details: |
        Create hub manifest with:
        - Category reference
        - Sub-agent list
        - Routing logic for planner agents
      status: "completed"
      result: "Created planner/manifest.yml with category reference to assets/definitions/agent-categories.yml#planner, sub-agent list (planner-build, planner-test, planner-cleaning, planner-teach, planner-agent-exam), and routing logic for domain-specific planning"

    - id: "planner-cat-02"
      description: "Update planner-build/inputs.yml to reference category"
      file: "planner/planner-build/inputs.yml"
      criteria: "inputs.yml references the shared category definition"
      status: "completed"
      result: "Added agent_context section with category reference to assets/definitions/agent-categories.yml#planner and role planner-build"

    - id: "planner-cat-03"
      description: "Update planner-cleaning/inputs.yml to reference category"
      file: "planner/planner-cleaning/inputs.yml"
      criteria: "inputs.yml references the shared category definition"
      status: "completed"
      result: "Added agent_context section with category reference to assets/definitions/agent-categories.yml#planner and role planner-cleaning"

    - id: "planner-cat-04"
      description: "Update planner-test/inputs.yml to reference category"
      file: "planner/planner-test/inputs.yml"
      criteria: "inputs.yml references the shared category definition"
      status: "completed"
      result: "Added agent_context section with category reference to assets/definitions/agent-categories.yml#planner and role planner-test"

    - id: "planner-cat-05"
      description: "Update planner-teach/inputs.yml to reference category"
      file: "planner/planner-teach/inputs.yml"
      criteria: "inputs.yml references the shared category definition"
      status: "completed"
      result: "Added agent_context section with category reference to assets/definitions/agent-categories.yml#planner and role planner-teach"

    - id: "planner-cat-06"
      description: "Update planner-agent-exam/inputs.yml to reference category"
      file: "planner/planner-agent-exam/inputs.yml"
      criteria: "inputs.yml references the shared category definition"
      status: "completed"
      result: "Added agent_context section with category reference to assets/definitions/agent-categories.yml#planner and role planner-agent-exam"

  validation:
    - "planner/manifest.yml created and references agent-categories.yml"
    - "All 5 sub-agents reference their category"
    - "All file references resolve correctly"
    - "No broken references"

  dependencies:
    - "planner_definitions.yml (def-01 must complete first)"
