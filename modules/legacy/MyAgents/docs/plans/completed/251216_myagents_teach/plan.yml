# Teach Refinement Plan
# Focus: Agent guidance refinement (text-only, no code)

worktree: /home/code/myagents/MyAgents-staging
plan_folder: docs/plans/251216_myagents_teach
created: 2025-12-16
status: active

live-plans:
  - id: MYAGENTS-TEACH-251216
    title: "Agent guidance refinement"
    description: |
      Improve agent guidance artifacts (processes, definitions, examples, manifests).
      Text-only refinements - no code implementation.

    scope:
      in:
        - "Process file clarity (process.yml files)"
        - "Definition accuracy (assets/definitions/)"
        - "Example quality (assets/examples/)"
        - "Manifest consistency (manifest.yml files)"
        - "Input specification completeness (inputs.yml files)"
      out:
        - "Code implementation"
        - "Test execution"
        - "Build/deployment"

    # COMPLETED: Infrastructure setup
    completed:
      - id: TEACH-INFRA-001
        title: "Teach entrypoint and orchestration"
        notes: "assets/processes/_teach.yml -> orchestration-teach -> planner-teach flow established"

      - id: TEACH-INFRA-002
        title: "Planning folder structure"
        notes: "docs/plans/251216_myagents_teach/ with plan.yml and plan_completed.yml"

      - id: TEACH-INFRA-003
        title: "Teacher manifest alignment"
        notes: "manifest-teacher.yml updated to reflect current sub-agents (teacher-process, teacher-update-assets)"

      - id: TEACH-INFRA-004
        title: ".claude/agents exposure"
        notes: "teacher-agent.md updated with correct process paths"

    # ACTIVE: Guidance refinement items
    phases:
      - name: Guidance Refinement
        notes: |
          Small, verifiable improvements to agent guidance.
          Each item targets specific files and has measurable acceptance criteria.
        items:
          # Add guidance refinement items here as friction is identified
          # Example format:
          # - id: TEACH-GUIDE-001
          #   title: "Clarify X process step Y"
          #   files: [path/to/process.yml]
          #   friction: "Agents repeatedly misunderstand step Y"
          #   acceptance: ["Step Y now specifies exact input files", "No ambiguous references"]
